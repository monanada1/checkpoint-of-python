C = 50
H = 30

# Split the input string into a list of strings
input_string = input("Enter the D values = ")
D_strings = input_string.split(',')

# Convert the list of strings into a list of integers
D_values = [int(d_string) for d_string in D_strings]

# Iterate over each value of D and calculate the corresponding Q value
for D in D_values:
    Q = int(((2*C*D)/H)**0.5);
    print(Q)
